---
label: Home
hide_body: true
fields:
- type: text
  name: title
  label: Page Title
  description: The title of the page
  config:
    required: true
- type: field_group
  name: sidebar
  label: Page Sidebar
  fields:
  - type: file
    name: primary_logo
    label: Primary Logo
    description: The image displayed above the sidebar menu when on a large screen
  - type: file
    name: mobile_logo
    label: Mobile Logo
    description: The image displayed to the left of the top menu bar when on a small
      screen
  - type: field_group_list
    name: entries
    label: Sidebar Entries
    description: The links displayed in the page sidebar
    fields:
    - type: text
      name: title
      label: Title
      description: The title displayed in the page sidebar
    - type: text
      name: url
      label: URL
      description: 'the URL (/generic, or /feugiat ...), or section ID (like #intro
        or #one ...) of the sidebar entry'
    - type: boolean
      name: is_primary
      label: Is Primary
      description: 'Underlines the Sidebar entry initially. note: only one entry can
        be highlighted at a time, so the first entry with this set will be selected'
      default: false
    config:
      labelField: title
- type: blocks
  name: sections
  label: Sections
  description: Home sections
  template_types:
  - intro
  - spotlights
  - features
  - contact
- type: text
  name: layout
  label: Layout
  hidden: true
  default: home
  config:
    required: true
pages:
- content/_index.md
